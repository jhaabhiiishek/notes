<%- include('partials/header') %>

<div class="jumbotron text-center">
  <div class="container">
    <i class="fas fa-key fa-6x"></i>
    <h1 class="display-3">Your personal Notes!</h1>
    <% for(var i=0;i<results.length;i++){ %>
      <% console.log(results[i].id)%>
    <p class="secret-text"><%= results[i].text %></p>
    <a class="btn btn-light btn-lg" href="/delete/<%= results[i].id%>" role="button">Delete</a>
    <hr>
    <% } %>
    
    <a class="btn btn-light btn-lg" href="/logout" role="button">Log Out</a>
    <a class="btn btn-dark btn-lg" href="/submit" role="button">Submit a Note</a>
  </div>
</div>

<%- include('partials/footer') %>
